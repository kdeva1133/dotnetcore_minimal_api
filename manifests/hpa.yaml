apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: apiworkflow2
  labels:
    app.kubernetes.io/name: apiworkflow2
    app.kubernetes.io/part-of: apiworkflow2-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: apiworkflow2
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80