apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: workflowaks
  labels:
    app.kubernetes.io/name: workflowaks
    app.kubernetes.io/part-of: workflowaks-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: workflowaks
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80